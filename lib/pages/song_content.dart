import 'package:amnesia_music_player/globals.dart';

class SongContentPage extends StatelessWidget {
  const SongContentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Song Content Page', style: AppStyles.mediumText)
      ),
    );
  }
}